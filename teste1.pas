program teste (input, output);
var n, k: integer;
    f1, f2, f3:boolean;

    procedure p(var a, x: boolean; var b: boolean);
    begin
        write(1);
    end;

    procedure p2(var a: boolean);
    begin
        write(3);
    end;
begin
end.

